The ability to be re-ordered is like a service for an Item, so it makes the most sense that they both implement the
same service through an interface. This is also the best option as both items will have slightly different
implementations of these methods because of the different threshold values.